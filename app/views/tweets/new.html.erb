<div class="container">
<div class="row p-5 mb-2 bg-light text-dark">
  <div class="col-4 offset-4">
      <h1>Que estas pensando, <%= current_user.user_name %>??</h1>
      <%= form_with(model: @tweet, local: true) do |form| %>
      <%= form.text_area :contents, size: "60x10" %>
      <span>
      <%= form.submit 'crear tweet', class:"btn btn-dark"%>
      </span>
      <% end %>
    </div>
  </div>
      <%= link_to 'volver al incio',root_path , class:"btn btn-dark ml-4" %>
</div>